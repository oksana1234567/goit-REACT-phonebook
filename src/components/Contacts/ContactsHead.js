import React from 'react';
import styles from '../Contacts/Contacts.module.css'

const ContactsHead = () => (
    <section>
        <h2 className={styles.ContactsHead}>Contacts</h2>
     </section>
     
);

export default ContactsHead;